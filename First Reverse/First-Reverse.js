function FirstReverse(str) { 
    // code goes here  
    // str = str.split('');
    // str.reverse();
    // str = str.join('');
    return str.split('').reverse().join('');
  }
     
  // keep this function call here 
  console.log(FirstReverse('Abcd'));